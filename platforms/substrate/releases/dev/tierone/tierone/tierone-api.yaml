apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tierone-api
  namespace: tierone-subs
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-dscpapp
        namespace: flux-dscpapp
      chart: examples/dscp-app/charts/dscp-api
  releaseName: tierone-api
  values:
    fullNameOverride: tierone-api
    config:
      port: 80
      externalNodeHost: "tierone"
      externalNodePort: 9944
      logLevel: info  
      externalIpfsHost: "tierone-ipfs-api" 
      externalIpfsPort: 5001 
      enableLivenessProbe: true
      substrateStatusPollPeriodMs: 10000
      substrateStatusTimeoutMs: 200000
      ipfsStatusPollPeriodMs: 10000
      ipfsStatusTimeoutMs: 200000
      auth:
        type: NONE
    ingress:
      enabled: false
      className: "gce"
      paths:
        - /v3
    replicaCount: 1
    image:
      repository: ghcr.io/digicatapult/dscp-api
      pullPolicy: IfNotPresent
      tag: 'v4.6.7'
    dscpNode:
      enabled: false

    dscpIpfs:
      enabled: false
      dscpNode:
        enabled: false
    vault:
      alpineutils: ghcr.io/hyperledger/alpine-utils:1.0
      address: http://vault.internal.demo.aws.blockchaincloudpoc.com:9001
      secretprefix: secretsv2/data/tierone-subs/tierone
      serviceaccountname: vault-auth
      role: vault-role
      authpath: substratetierone
