apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: carrier-restserver
  namespace: carrier-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  releaseName: carrier-restserver
  interval: 1m
  chart:
   spec:
    chart: examples/supplychain-app/charts/fabric-restserver
    sourceRef:
      kind: GitRepository
      name: flux-demo-fabric-app
      namespace: flux-demo-fabric-app
  values:
    metadata:
      namespace: carrier-net
    server:
      name: carrier-restserver
      port: 20001
      localmspid: carrierMSP
      image: ghcr.io/hyperledger/bevel-supplychain-fabric:rest-server-v1
      username: user1
      cert_path: "/secret/tls/user1.cert"
      key_path: "/secret/tls/user1.pem"
    storage:
      storageclassname: carrier-aws-storageclass
      storagesize: 512Mi
    vault:
      address: http://vault.internal.demo.aws.blockchaincloudpoc.com:9001
      role: vault-role
      authpath: demo-fabriccarrier-net-auth
      secretprefix: secretsv2/data/crypto/peerOrganizations/carrier-net
      serviceaccountname: vault-auth
      imagesecretname: regcred
      image: ghcr.io/hyperledger/alpine-utils:1.0
    service:
      servicetype: ClusterIP
      ports:
        apiPort: 20001
        targetPort: 8000
    connection:
      peer: peer0.carrier-net
      peerAddress: peer0.carrier-net
      peerPort: 7051
      ordererAddress: orderer1.hf.demo.aws.blockchaincloudpoc.com
      ordererPort: 443
