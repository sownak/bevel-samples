apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: manufacturer-expressapi
  namespace: manufacturer-ns
  annotations:
    fluxcd.io/automated: "false"
spec:
  chart:
    spec:
      chart: examples/supplychain-app/charts/expressapp
      sourceRef:
        kind: GitRepository
        name: flux-corda-app
        namespace: flux-corda-app
  releaseName: manufacturer-expressapi
  interval: 1m
  values:
    expressapp:
      image: ghcr.io/hyperledger/bevel-supplychain-corda:express-app-latest
      pullPolicy: Always
      pullSecrets: regcred
      nodePorts:
        port: 3000
        targetPort: 3000
      apiUrl: https://manufacturer-springboot.manufacturer-ns:20001/api/v1
    proxy:
      provider: ambassador
      name: manufacturer
      type: corda
      external_url_suffix: rc.demo.blockchaincloudpoc.com
