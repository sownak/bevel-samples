apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: warehouse-frontend
  namespace: warehouse-ns
  annotations:
    fluxcd.io/automated: "false"
spec:
  releaseName: warehouse-frontend
  interval: 1m
  chart:
   spec:
    chart: examples/supplychain-app/charts/frontend
    sourceRef:
      kind: GitRepository
      name: flux-corda-app
      namespace: flux-corda-app
  values:
    frontend:
      nodePorts:
        port: 8080
        targetPort: 80
      image: ghcr.io/hyperledger/bevel-supplychain-frontend:latest
      pullPolicy: Always
      pullSecrets: regcred
      apiURL: https://warehouseapi.rc.demo.blockchaincloudpoc.com
    deployment:
      annotations: {}
    proxy:
      provider: ambassador
      peer_name: warehouse
      external_url_suffix: rc.demo.blockchaincloudpoc.com
